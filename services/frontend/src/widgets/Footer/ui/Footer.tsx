import type React from "react";
import type { BaseProps } from "@/shared/types/components";

export const Footer: React.FC<BaseProps> = ({ className }) => {
	return <footer className={className}></footer>;
};
