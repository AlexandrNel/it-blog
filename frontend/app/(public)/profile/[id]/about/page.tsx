import { AboutPage } from "@/views/profile";
import { type Metadata } from "next";

export const metadata: Metadata = {
  title: "О профиле",
  description: "Информация об авторе в IT Blog.",
};

export default AboutPage;
